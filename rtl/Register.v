module Register(
    input  clk, load,
    input  [15:0] in,
    output reg [15:0] out
);
    // TODO 1: Create a synchronous load register
    // TODO 2: Ensure out retains its value when load is 0
endmodule
